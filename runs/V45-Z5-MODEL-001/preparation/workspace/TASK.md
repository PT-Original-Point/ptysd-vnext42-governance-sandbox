# Z5 synthetic task

Modify only `src/message.txt` so its exact UTF-8 content is:

```text
HELLO_FROM_OPENCODE
```

Do not modify any other file. Do not use network tools. Do not run shell commands. The trusted outer verifier will run the fixed test.
