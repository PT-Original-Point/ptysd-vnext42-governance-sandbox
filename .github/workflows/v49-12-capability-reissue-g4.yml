name: V49-12 capability reissue g4

on:
  push:
    branches:
      - v49-12-capability-reissue-g4-20260919
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Compile exact generation-4 capability
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import {compileCellCapability,assertCapabilityCurrent} from './tools/csg/cell-kernel/production-kernel.mjs';
          const contract=JSON.parse(fs.readFileSync('.qualification/v49-12/contract.json','utf8'));
          const fence=JSON.parse(fs.readFileSync('.qualification/v49-12/fence-g4.json','utf8'));
          const cap=compileCellCapability(contract,fence);
          assertCapabilityCurrent(cap,fence);
          if(!cap.execution_authorized) throw new Error('EXECUTION_NOT_AUTHORIZED');
          if(cap.fence_generation!==4) throw new Error('GENERATION_DRIFT');
          if(cap.owned_paths.join(',')!=='fixtures/v49-live-n1/src/slugify.mjs') throw new Error('OWNED_PATH_DRIFT');
          if(cap.resource_limits.provider_calls!==1) throw new Error('PROVIDER_CALL_BUDGET_DRIFT');
          if(cap.resource_limits.network_mode!=='LIMITED') throw new Error('NETWORK_MODE_DRIFT');
          console.log('V49_G4_CAPABILITY='+JSON.stringify(cap));
          NODE
